// Integrating scales: POP, Regional, and Global

function generatePlanet() {
    // Existing code...

    // Scale: POP
    function generatePopScale() {
        // Implementation for populating at the local scale
    }

    // Scale: Regional
    function generateRegionalScale() {
        // Implementation for populating at the regional scale
    }

    // Scale: Global
    function generateGlobalScale() {
        // Implementation for populating at the global scale
    }

    // Call the scale functions as needed
    generatePopScale();
    generateRegionalScale();
    generateGlobalScale();

    // Existing code...
}